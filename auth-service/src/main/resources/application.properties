spring.application.name=auth-service
server.port=8009

# Configuración de la BBDD
spring.datasource.url=jdbc:mysql://localhost:3306/authdb?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=SysPass99***
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect  # ✅ CORREGIDO

# JWT
jwt.secret=mi_clave_super_secreta
jwt.expiration=3600000

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# H2
spring.h2.console.enabled=false

# Logging
logging.level.org.springframework.web=DEBUG
